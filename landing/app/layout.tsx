import type { Metadata } from "next";
import localFont from "next/font/local";
import "./globals.css";

const geistSans = localFont({
  src: "./fonts/GeistVF.woff",
  variable: "--font-geist-sans",
  weight: "100 900",
});
const geistMono = localFont({
  src: "./fonts/GeistMonoVF.woff",
  variable: "--font-geist-mono",
  weight: "100 900",
});

export const metadata: Metadata = {
  title: "InsightGlass — Empathetic AI for Human Connection",
  description:
    "InsightGlass uses on-device AI and Meta Ray-Ban glasses to offer soft, speculative emotional observations that help you connect more thoughtfully.",
  openGraph: {
    title: "InsightGlass",
    description: "On-device AI empathy assistant for iPhone + Meta Ray-Ban glasses.",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{ children: React.ReactNode }>) {
  return (
    <html lang="en" className="scroll-smooth">
      <body className={`${geistSans.variable} ${geistMono.variable} antialiased`}>
        {children}
      </body>
    </html>
  );
}
